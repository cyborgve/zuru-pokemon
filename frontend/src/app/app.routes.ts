import { Routes } from '@angular/router';
import { PokemonSearchComponent } from './features/pokemon-search/pokemon-search.component';

export const routes: Routes = [
  { path: '', component: PokemonSearchComponent }
];
